import 'package:flutter/material.dart';

class TextPage2 extends StatelessWidget {
  const TextPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("text1"),
    );
  }
}
